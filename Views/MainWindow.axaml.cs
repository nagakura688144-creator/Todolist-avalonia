using Avalonia.Controls;

namespace TodoApp.Views;

public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
        // No code-behind event handlers needed; everything is bound via MVVM in XAML.
    }
}
